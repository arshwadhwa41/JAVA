package java_questions.CodeChallange5;

public class FindListMiddle {
    static Node middle(Node head){
        Node slow=head;
        Node fast=head;
        while(fats!=null && fast.next!=null){
            slow=slow.next;
            fast=fsat.next.next;
        }
        return slow;
    }
}
